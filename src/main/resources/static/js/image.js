function frontview(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function fontview(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#two')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function fntview(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#three')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one2')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front1(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one3')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front2(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one4')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front3(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one5')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front4(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one6')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front5(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one7')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front6(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one8')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}
function front7(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $('#one9')
                .attr('src', e.target.result)

        };

        reader.readAsDataURL(input.files[0]);
    }
}